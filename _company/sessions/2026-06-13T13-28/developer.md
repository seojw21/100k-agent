# 💻 코다리 — 디자인 가이드를 바탕으로 실시간 'Lost Income' 계산 파이프라인을 시스템에 통합하고 데이터 연동 및 자동화 검증을 완료하십시오.

💻 코다리: 작업 시작합니다.

CEO의 지시사항과 Designer의 가이드를 확인했습니다. 핵심은 **'Lost Income' 계산 로직을 단순한 UI 수치가 아닌, 실제 데이터와 연동되는 자동화 파이프라인으로 구축**하는 것입니다.

먼저 현재 시스템 내에 T+72 관련 로직이나 기존 계산 엔진이 어디에 위치해 있는지 파악하고, 설계된 가이드(#000000, #FFD700)가 반영될 수 있는 구조를 확인하겠습니다.

1. **탐색**: `T+72`, `Lost Income`, `calculation` 키워드로 관련 파일 탐색.
2. **분석**: 기존 계산 로직 유무 및 데이터 소스(사용자 입력값 vs DB 데이터) 파악.
3. **구현**: 
   - `Time Saved`와 `Hourly Rate`를 기반으로 한 `Lost Income` 계산 모듈 구축.
   - 예외 처리(음수 값, 빈 값 등)를 포함한 견고한 로직 설계.
4. **검증**: 단위 테스트를 통해 계산 결과가 정확한지 확인.

먼저 관련 파일들을 탐색하겠습니다.

<run_command>grep -rnE "T+72|Lost Income|calculation" .</run_command>

---
## 🛠️ 도구 실행 결과 (시스템 자동 실행)

### 🔧 실행: `grep -rnE "T+72|Lost Income|calculation" .`
```

```
_❌ exit 1_
